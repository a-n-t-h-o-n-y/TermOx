#include <zzz/test.hpp>

#include <ox/core/core.hpp>

TEST(terminal_construction)
{
    // struct {
    //     ox::Point coordinates = {.x = 5, .y = 3};
    //     ox::Area size = {.width = 40, .height = 20};
    // } widget;

    // auto sb = ox::ScreenBuffer{{.width = 20, .height = 10}};

    // auto c = ox::Painter{widget};
}